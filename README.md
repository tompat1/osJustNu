# osJustNu
Håll koll på allt svenskt i OS just nu

## Preview
Start a local web server from the repo root and open the site in your browser:

```bash
python -m http.server 8000
```

Then visit `http://localhost:8000/` (or the forwarded port in your environment).
